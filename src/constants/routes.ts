export enum AuthRoutes {
	Login = "/auth/login",
	Register = "/auth/register",
}

export enum NavRoutes {
	AboutUs = "/about-us",
	Marketplace = "/marketplace",
	Contacts = "/contacts",
	Rankings = "/rankings",
	Profile = "/profile",
}
