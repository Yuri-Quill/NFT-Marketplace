import Container from "../Container/Container";
import "./Header.scss";

const Header = () => {
	return (
		<header>
			<Container className="header">
				<nav></nav>
			</Container>
		</header>
	);
};

export default Header;
